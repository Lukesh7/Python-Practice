import pandas as pd
import numpy as np

#series - single column
s = pd.Series([10,20,30])
print(s)

#DataFrame - from dictionary
data = {
    "users": ['user1', 'user2', 'user3'],
    'age' : [21, 23, 25]
}
df = pd.DataFrame(data)
print(df)

#DataFrame - from numpy arr
arr = np.array([[1,2], [3,4]])

df = pd.DataFrame(arr, columns =['A', 'B'])
print(df)
